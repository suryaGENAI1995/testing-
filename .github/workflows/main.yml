name: Hello AIForce Test

on:
  push:
    paths:
      - '**.txt'  # Trigger this test only when a .txt file is pushed

jobs:
  hello-job:
    runs-on: ubuntu-latest

    steps:
    - name: 📥 Checkout repo
      uses: actions/checkout@v3

    - name: 👋 Say Hello
      run: echo "Hello, AIForce! 🚀 GitHub Actions is working!"
